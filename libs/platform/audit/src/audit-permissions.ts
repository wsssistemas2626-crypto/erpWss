/** Permissões da auditoria, registradas no catálogo sob o módulo `platform`. */
export const AUDIT_PERMISSIONS = [
  { key: 'platform.audit.read', description: 'Consultar a trilha de auditoria' },
] as const;
